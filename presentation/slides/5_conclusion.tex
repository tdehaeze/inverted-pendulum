% Le titre de la partie
\section{Conclusion}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Première diapo avec un exemple de tableau
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{frame}
\frametitle{Conclusion}

\textbf{Bilan}
\begin{itemize}
  \item Pendule inversé sur chariot comme support d'étude du contrôle actif d'une dynamique instable
  \item Deux stratégies de contrôle comparées :
  \begin{itemize}
    \item \textbf{PD} — intuitif, paramètres à sens physique clair (gain, amortissement), mais ne stabilise pas la position du chariot
    \item \textbf{LQR} — optimal au sens d'un critère quadratique, s'appuie sur un modèle linéarisé, contrôle simultané de $\varphi$ et $x$
  \end{itemize}
\end{itemize}

\vspace{0.4cm}

\textbf{Perspectives}
\begin{itemize}
  \item \textbf{LQG} — ajout d'un filtre de Kalman pour l'estimation des états bruités ($\dot{x}$, $\dot{\varphi}$) ; extension naturelle du LQR
  \item \textbf{MPC} (commande prédictive) — optimisation sur un horizon glissant, permet de prendre en compte explicitement certaines contraintes physiques
\end{itemize}

\end{frame}

\begin{frame}[allowframebreaks]{Références}
  \printbibliography[heading=none]
\end{frame}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% SLIDES DE BACKUP
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\appendix

\begin{frame}
\frametitle{Annexe — Matériel}
\framesubtitle{Récupération d'une imprimante 3D Alfawise U20}

\begin{center}
    \includegraphics[height=6.5cm]{figures/alfawise-u20.jpg}
\end{center}

\end{frame}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\begin{frame}
\frametitle{Annexe — Identification du modèle}
\framesubtitle{Réponse libre — masse 100\,g}

\begin{center}
    \includegraphics[width=0.9\linewidth]{figures/free_response_100g_fit.png}
\end{center}

\vspace{0.2cm}
{\small
Avec une masse plus importante, la friction est négligeable devant l'inertie :
le modèle linéarisé reproduit fidèlement la réponse libre expérimentale.
}

\end{frame}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\begin{frame}
\frametitle{Annexe — Équivalence LQR / PD}
\framesubtitle{Le LQR comme double correcteur PD}

\begin{center}
\begin{tikzpicture}[>=Stealth,
    block/.style={draw, rectangle, rounded corners=3pt, fill=blue!8,
                  minimum height=0.85cm, minimum width=1.7cm,
                  align=center, font=\small},
    plant/.style={draw, rectangle, rounded corners=3pt, fill=blue!8,
                  minimum height=2.1cm, minimum width=1.7cm,
                  align=center, font=\small},
    sum/.style={draw, circle, minimum size=0.55cm, inner sep=0pt},
  ]

  \node[sum]   (sum_phi) at (0,   0.85) {};
  \node[sum]   (sum_x)   at (0,  -0.85) {};
  \node[block] (pd_phi)  at (2.3,  0.85) {PD$_\varphi$};
  \node[block] (pd_x)    at (2.3, -0.85) {PD$_x$};
  \node[sum]   (sum_u)   at (4.0,  0)    {};
  \node[plant] (plant)   at (6.0,  0)    {Pendule\\inversé};

  % +/- labels
  \node[font=\scriptsize, above left=-5pt and 2pt of sum_phi] {$+$};
  \node[font=\scriptsize, above left=2pt and -5pt of sum_phi] {$-$};
  \node[font=\scriptsize, above left=-5pt and 2pt of sum_x] {$+$};
  \node[font=\scriptsize, below left=2pt and -5pt of sum_x]   {$-$};
  \node[font=\scriptsize, above left=2pt and -5pt of sum_u]   {$+$};
  \node[font=\scriptsize, below left=2pt and -5pt of sum_u]   {$+$};

  % References
  \draw[->] (-1.5,  0.85) node[above right,font=\small] {$\theta_\mathrm{ref}$} -- (sum_phi.west);
  \draw[->] (-1.5, -0.85) node[above right,font=\small] {$x_\mathrm{ref}$} -- (sum_x.west);

  % Forward path
  \draw[->] (sum_phi.east) -- node[above,font=\scriptsize] {$\varepsilon_\varphi$} (pd_phi.west);
  \draw[->] (sum_x.east)   -- node[above,font=\scriptsize] {$\varepsilon_x$}       (pd_x.west);
  \draw[->] (pd_phi.east)  -| (sum_u.north);
  \draw[->] (pd_x.east)    -| (sum_u.south);
  \draw[->] (sum_u.east)   -- node[above,font=\scriptsize] {$u = \ddot{x}$} (plant.west);

  % Plant outputs
  \coordinate (phi_out) at ($(plant.east)+(0,  0.45)$);
  \coordinate (x_out)   at ($(plant.east)+(0, -0.45)$);
  \draw[->] (phi_out) -- ++(0.75, 0) node[right,font=\small] {$\theta$};
  \draw[->] (x_out)   -- ++(0.75, 0) node[right,font=\small] {$x$};

  % Feedback paths (routed below the diagram)
  \draw[->] ($(phi_out)+(0.35,0)$)node{$\bullet$} -- ++(0, 1.2) -| (sum_phi.north);
  \draw[->] ($(x_out)+(0.35,0)$)node{$\bullet$}   -- ++(0,-1.2) -| (sum_x.south);
  % \draw[->] ($(x_out)  +(0.35,0)$) -- ++(0,-2.3) -- ($(sum_x.south)  +(0,-2.3)$) -- (sum_x.south);
\end{tikzpicture}
\end{center}

\vspace{1em}
    \[u = \underbrace{-k_\varphi\,\varepsilon_\varphi - k_{\dot\varphi}\,\dot\varphi}_{\text{PD}_\varphi} \underbrace{- k_x\,\varepsilon_x - k_{\dot x}\,\dot x}_{\text{PD}_x},
\quad\quad
    \text{LQR}: \quad \mathbf{K} = \begin{bmatrix} k_x & k_{\dot x} & k_\varphi & k_{\dot\varphi} \end{bmatrix} \]

\end{frame}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\begin{frame}[fragile]
\frametitle{Annexe — Code}
\framesubtitle{Identification des paramètres du modèle (\texttt{tools/fit.py})}

Minimisation des moindres carrés entre simulation et mesure :

\begin{lstlisting}[style=python]
def simulate_free(t_eval, theta0, omega0, l, c, m):
    D = (1/12)*m*(2*l)**2 + m*l**2   # = 4/3 * m * l^2
    def ode(t, y):
        theta, omega = y
        return [omega, (-c*omega - m*G*l*np.sin(theta)) / D]
    sol = solve_ivp(ode, (t_eval[0], t_eval[-1]), [theta0, omega0], t_eval=t_eval)
    return sol.y[0]

def cost(params, t, theta_meas, m):
    l, c = params
    theta_sim = simulate_free(t, theta_meas[0], 0.0, l, c, m)
    return float(np.mean((theta_sim - theta_meas)**2))

result = minimize(cost, [l_init, c_init], args=(t, theta, m), method="Nelder-Mead")
l_fit, c_fit = result.x
\end{lstlisting}

\vspace{0.1cm}
{\small Algorithme de Nelder-Mead sur les paramètres $(l, c)$, masse $m$ fixée.}

\end{frame}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\begin{frame}[fragile]
\frametitle{Annexe — Code}
\framesubtitle{Simulation du système (\texttt{model/inverted\_pendulum.py})}

\begin{columns}[T]
  \begin{column}{0.48\textwidth}
    Dynamique non-linéaire :
\begin{lstlisting}[style=python]
def dynamics(self, state, u):
    x, v, theta, omega = state
    D = p.I + p.m*p.l**2
    domega = (- p.c*omega
         - p.m*p.g*p.l*np.sin(theta)
         - p.m*p.l*u*np.cos(theta)) / D
    return np.array([v, u, omega, domega])
\end{lstlisting}
    Intégration Euler, $dt = 1\,\text{ms}$ :
\begin{lstlisting}[style=python]
for k in range(N):
    u = controller(t[k], x)
    dx = plant.dynamics(x, u)
    x  = x + dx * dt
    history[k] = x
\end{lstlisting}
  \end{column}
  \begin{column}{0.48\textwidth}
    Synthèse LQR :
\begin{lstlisting}[style=python]
Q = np.diag([q_x, 0, q_phi, 0])
R = np.array([[1]])
K, P, E = control.lqr(A, B, Q, R)
\end{lstlisting}
    \vspace{0.2cm}
    Contrôleur LQR :
\begin{lstlisting}[style=python]
def __call__(self, t, state):
    phi_state = state.copy()
    phi_state[2] = state[2] - np.pi
    u = float((-self.K @ phi_state).item())
    return u
\end{lstlisting}
  \end{column}
\end{columns}

\end{frame}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\begin{frame}[fragile]
\frametitle{Annexe — Code}
\framesubtitle{Contrôleur embarqué — Teensy 4.1 (\texttt{teensy/src/main.cpp})}

Boucle de contrôle à 1\,kHz via \texttt{IntervalTimer} :

\begin{lstlisting}[style=cpp]
// Convert to SI units
float phi_rad     = (pend_deg   - theta_ref_deg) * (PI / 180.0f);
float phi_dot_rad =  pend_vel_deg_s              * (PI / 180.0f);
float x_m         = (cart_mm    - x_ref_mm)      / 1000.0f;
float x_dot_m     =  cart_vel_mm_s               / 1000.0f;

// Controller output [m/s^2]
switch (ctrl_mode) {
    case MODE_PD:
        accel_m_s2 = -Kp * phi_rad - Kd * phi_dot_rad;  break;
    case MODE_LQR:
        accel_m_s2 = -(K[0]*x_m + K[1]*x_dot_m + K[2]*phi_rad + K[3]*phi_dot_rad);  break;
}

// Soft braking: block acceleration toward cart limits (+/-160 mm)
if (cart_mm >  CART_SOFT_MM && accel_cmd > 0.0f) accel_cmd = 0.0f;
if (cart_mm < -CART_SOFT_MM && accel_cmd < 0.0f) accel_cmd = 0.0f;

cart_vel_mm_s += accel_cmd * DT;   // integrate acceleration -> velocity
\end{lstlisting}

\end{frame}

